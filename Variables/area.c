#include<stdio.h>
#include<conio.h>
int main()
{
    float r,pi=3.14,area;
   printf("Enter radius values");
    scanf("%f",&r);
    area=pi*r*r;
    printf("Area is=%f",area);
    return 0;
}